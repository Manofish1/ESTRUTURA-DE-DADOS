#include <stdio.h>
#include <string.h>

struct livro {
    char autor[50]; //%s
    char titulo[50]; 
    int paginas; //d
};

int main()
{
    struct livro harry;
    strcpy (harry.autor, "JK");
    strcpy (harry.titulo, "bruxo");
    harry.paginas = 25;
    printf("autor: %s\ntitulo: %s\npaginas: %d\n", harry.autor, harry.titulo, harry.paginas);
// usar %s em char e %d em int
    return 0;
}
///////////////////////////////////////////////////////////////////////////////////////////////

#include <stdio.h>

int main() {
  int c;
  int d;
  int *ptr;
  int i;
  
  printf("o valor de C:\n ");
  scanf("%d",&c);
  ptr = &c;
    *ptr = 100;
    
    printf("o valor de D:\n ");
  scanf("%d",&d);
  ptr = &d;
    *ptr = 150;
    
    printf("C vale %d\n", c);
    printf("D vale %d\n", d);
    
    printf("HAHA trouxa mudei seus valores\n");
   
    return 0;
}
